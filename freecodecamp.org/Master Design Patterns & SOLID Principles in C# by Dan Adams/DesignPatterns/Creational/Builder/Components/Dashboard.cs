namespace DesignPatterns.Creational.Builder.Components;

public class Dashboard
{
    private bool _hasRevCounter;

    public Dashboard(bool hasRevCounter)
    {
        _hasRevCounter = hasRevCounter;
    }
}