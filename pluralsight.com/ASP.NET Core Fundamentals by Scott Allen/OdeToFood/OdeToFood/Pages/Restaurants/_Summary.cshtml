@using OdeToFood.Core
@model Restaurant

<!-- partial view generated with no model using `dotnet aspnet-codegenerator razorpage _Summary Empty -partial -npm -outDir=Pages/Restaurants` -->

<div class="card bg-light mb-3">
    <div class="card-header">
        <h2 class="card-title">@Model.Name</h2>
    </div>
    <div class="card-body">
        <span>Location: @Model.Location</span>
        <span>Cuisine: @Model.Cuisine</span>
    </div>
    <div class="card-footer">
        <!-- 'asp-route-<property name>' specifies a value for an input property on the target page -->
        <a class="btn btn-lg btn-light" asp-page="./Detail" asp-route-restaurantId="@Model.Id">
            <i>Details</i>
        </a>
        <a class="btn btn-lg btn-light" asp-page="./Edit" asp-route-restaurantId="@Model.Id">
            <i>Edit</i>
        </a>
        <a class="btn btn-lg btn-light" asp-page="./Delete" asp-route-restaurantId="@Model.Id">
            <i>Delete</i>
        </a>
    </div>
</div>